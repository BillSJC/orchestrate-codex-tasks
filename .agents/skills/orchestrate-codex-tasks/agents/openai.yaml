interface:
  display_name: "Codex Task Orchestrator"
  short_description: "Coordinate independent Codex Controller and Worker tasks"
  default_prompt: "Use $orchestrate-codex-tasks to split this request into independent Codex Worker tasks, coordinate them, and synthesize the verified result."

policy:
  allow_implicit_invocation: true
